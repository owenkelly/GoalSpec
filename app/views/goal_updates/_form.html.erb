<%= form_for([@goal, @goal_update]) do |f| %>
  <% if @goal_update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_update.errors.count, "error") %> prohibited this goal_update from being saved:</h2>

      <ul>
      <% @goal_update.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry %><br>
    <%= f.text_area :entry %>
    <%= f.hidden_field :goal_id, {:value => @goal.id} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
