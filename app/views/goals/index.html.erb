<h1>Listing goals</h1>

<table>
  <thead>
    <tr>
      <th>Goal</th>
      <th>Reason</th>
      <th>Start</th>
      <th>End</th>
      <th>Recurrance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @goals.each do |goal| %>
      <tr>
        <td><%= goal.goal %></td>
        <td><%= goal.reason %></td>
        <td><%= goal.start %></td>
        <td><%= goal.end %></td>
        <td><%= goal.recurrance %></td>
        <td><%= link_to "Create SubGoal", new_sub_goal_path(:goal_id => goal) %>
        <td><%= link_to 'Show', goal %></td>
        <td><%= link_to 'Edit', edit_goal_path(goal) %></td>
        <td><%= link_to 'Destroy', goal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Goal', new_goal_path %>
